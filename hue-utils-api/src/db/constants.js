const NO_SCHEMA = "Must provide schema configuration";
const NO_CONNECTION = "Connection object not in expected format";

const NO_CONFIG = "Must provide db config";
const SUCCESS_PREFIX = "Successfully connected to";
const DB_CONNECT_ERROR_PREFIX = "db connect error";

module.exports = {
  NO_SCHEMA,
  NO_CONNECTION,
  NO_CONFIG,
  SUCCESS_PREFIX,
  DB_CONNECT_ERROR_PREFIX,
};
